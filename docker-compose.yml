version: '3.8'

services:
    db:
        image: postgres:latest
        restart: always
        environment:
            POSTGRES_DB: food_explorer
            POSTGRES_USER: your_database_user
            POSTGRES_PASSWORD: your_database_password
